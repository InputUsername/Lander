Lander is (going to be) a really simple static site generator. It's written in Lua.
It will use only Lua (and Markdown to write blog posts). Heavily inspired by Jekyll.

Unfortunately requires Lua 5.1 due to a library that doesn't work on 5.3.
